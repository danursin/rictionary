import Layout from "../components/Layout";
import Navbar from "../components/Navbar";
import React from "react";

const Home: React.FC = () => {
    return (
        <Layout>
            <p>I'm the index inside the layout</p>
        </Layout>
    );
};

export default Home;
